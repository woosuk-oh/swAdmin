import React, {Component} from 'react';
import PropTypes from 'prop-types';

const propTypes = {};
const defaultProps = {};

class sliceMessage extends Component {
    constructor(props)
    {
        super(props);
    }
    
    render()
    {
        return (
            <div>sliceMessage</div>
        );
    }
}
sliceMessage.propTypes = propTypes;
sliceMessage.defaultProps = defaultProps;


export default sliceMessage;
