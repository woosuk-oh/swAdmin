import React, {Component} from 'react';
import PropTypes from 'prop-types';

const propTypes = {};
const defaultProps = {};

class deleteTag extends Component {
    constructor(props)
    {
        super(props);
    }
    
    render()
    {
        return (
            <div>deleteTag</div>
        );
    }
}
deleteTag.propTypes = propTypes;
deleteTag.defaultProps = defaultProps;


export default deleteTag;
